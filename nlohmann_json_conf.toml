[project]
name = "json"

git_repo = "nlohmann/json"

mokai_toml = """"
[project]
name = "json"
description = "JSON for Modern C++"

[exports]
default_targets = ["json_interface"]
include_dirs = ["include"]

[target.json_interface]
type = "static_library" 
sources = []
include_dirs = ["include"]
"""
